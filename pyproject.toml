[project]
name = "cadair"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "aiohttp>=3.13.5",
    "ezdxf>=1.4.3",
    "python-dotenv>=1.2.2",
]

[project.scripts]
cadair = "cadair.cli:main"
